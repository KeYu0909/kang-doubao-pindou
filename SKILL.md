---
name: kang-doubao-pindou
description: 将用户图片分阶段制作成真实拼豆网格、像素预览、正式 PNG 和独立 CSV/PDF。支持固定脚本快速减色、本地三档去杂色、特征保护、自然确认及持久环境恢复。仅讨论或开发代码时不调用图片工具。
license: MIT; third-party notices in licenses/
metadata:
  author: 永康玩AI
  version: "0.3.1"
  compatibility: Python 3.10+ on Linux/macOS (Windows via WSL), pinned dependencies and local files. Doubao native tools, durable storage and delivery must be verified on the actual host.
---

# 永康玩AI · 拼豆助手

**AI 负责理解与建议、做选择，程序负责做执行，用户负责做确认。**

快速交付可确认结果优先于自主追求完美。当前阶段有满足基本要求的候选后立即交付，不因为“感觉还能优化”“想再检查一下”自行继续分析、重做、比较多版本或切换备用方案。只有技术失败、必要自动校验失败或用户明确修改才继续处理；每轮生成式图片调用最多一次，不自动重试。

去杂色、配色、网格、PNG、CSV、PDF 等确定性任务使用固定脚本，不调用生成式 AI。只有图片理解、创意编辑和模糊用户意图需要 AI。指引全部中文，技术色号、命令、版本号保留原标识。

## 首次使用与新窗口：先确定安装和入口

首次制作前提示：“我会先检查持久化安装和新窗口入口，避免换窗口后找不到技能。”按[持久化安装](references/persistent-install.md)执行。首次安装、更新及新窗口恢复只做一次检查，正常制图不重复安装。

先查当前项目的 `pindou-install.json` 或项目说明中的实际安装记录，从记录的绝对路径读取本版 SKILL.md 并检查环境。找不到记录时才检查当前技能目录。`doctor` 的 `ok/READY` 仅表示当前依赖可用；必须同时读取 `storage`，不得据此宣布持久化安装成功。

发现技能或依赖仍在 `/runtime` 等临时目录，先将完整源码安装到宿主确认保留的最终目录，在最终位置创建虚拟环境，保留原作品；不要搬动旧 venv 或只建立临时软链接。按文档用 `install_record.py record` 保存实际路径到项目文件，并让宿主在原项目的新窗口能够读取该记录。无法确定保留位置时，只说明本会话可用及备份方式，不虚构稳定路径。

**文件保留与技能入口注册分开验证。** 用宿主真实的技能安装/项目绑定入口确认可发现范围；项目文件记录和软链接都不能代替技能注册。新窗口打开同一项目，重新选择技能或读取项目安装记录，检查真实路径仍能运行后才报告已验证的范围。独立项目/新沙箱不保证共享安装；不得声称全账号永久可用。没有宿主入口能力时说明需要用户在技能界面选择或绑定。

已有环境用实际 Python 和脚本绝对路径执行 `doctor --repair`，只修复必要依赖；缺临时链接可加 `--link`，但它只是当前入口。后续直接复用记录路径。恢复作品检查真实 state.json/revisions，不凭聊天重建；普通处理无需先 status 再思考再执行。

## 阶段 0：只读图，快速建议

用户仅上传图片时，快速看图并给一句建议，不马上处理、不写临时代码、不抠图、不做多项视觉检查。复杂背景可给：1. 整理背景（推荐）；2. 整理背景并加创意元素；3. 保持原图直接转换。图片干净则推荐直接转换。允许用户直接说自定义要求，等待选择；明确要求不重复问。

目标 5–15 秒，仅为待实测目标。

## 阶段 1：一次素材图整理

将去背景、去无关元素、帽子、眼镜、配饰等明确要求合成一次编辑。优先调用[当前宿主真实可用的图片编辑能力](references/doubao-adapter.md)，不要现场写抠图代码，不虚构豆包接口。只改用户要求，保留姿态、毛色、眼睛等特征，主体完整，不裁掉耳朵/帽子/尾巴/手脚；输出干净素材，不加文字、不做网格或拼豆效果。

用 `init` 保存原图；`begin --current --kind edit --request ... --preserve ...` 绑定实际输入；`claim-call` 后调用一次宿主工具，用 `edit --input` 导入返回文件。完成技术校验后立即展示一张候选，等待素材确认。详细命令见[快速操作](references/fast-path.md)。没有图片编辑工具时明确说明并接受用户提供的素材，不悄悄换方案。原有普通本地裁切等低级命令仅保留兼容和明确指定用途。

用户选择保持原图，则展示同一原图候选并确认，随后走本地转换。编辑目标 20–60 秒；工具超过60秒如实记录为宿主瓶颈。

## 阶段 2：真实像素预览与本地建议

素材确认后一次 `pixel --current`，沿用参数，缺参建议78×78、MARD、上限18色（可修改）。不在像素阶段再生图或重新长时间读图。

脚本转换、配色、保存真实 cells、生成预览并分析小色块。**先交付返回的 preview_small 和完整 preview**，简短报告尺寸、实际色数、颗数及 `analysis.hint`。不得把生成文件或助手自己打开图当成用户已看到。

杂色建议仅依据 `analysis`，详细区域在 `files.analysis`，普通回合不要读取整份大 JSON。预计修改格数只能说“预计”。发现可保守处理的杂色时推荐轻度；没有合格候选则说可以保持。四个选项：轻度、平衡、强力、保持。强力只在用户希望更好拼/减少颜色时建议，并明确询问目标色数，不擅自减色。

去杂色是可选修改，不必先确认将被替换的旧像素预览。用户确认保留最终像素版后才能生成 PNG。

## Fast Path：明确要求直接执行

实际展示后用 `present --current --conversation ... --artifact candidate|preview|pattern --message-ref ...` 记录真实交付。展示菜单后用 `offer --current --options ...` 绑定菜单；通常2–3项，去杂色菜单允许4项。

所有短答和清晰请求优先 `route --current --conversation ... --message '用户原话' --message-ref ...`，不另调模型分类。一次命令内部取当前状态并执行：

| 用户要求 | 固定动作 |
|---|---|
| 18色改12色 / 颜色减到12色 | 当前网格重新配色，保留已有去杂色结果，不读原图、不调用 AI |
| 轻度去杂色 / 去一点杂色 | 本地轻度处理，生成新 revision 与真实预览 |
| 平衡去杂色 | 本地平衡处理 |
| 强力简化到12色 | 按明确上限收敛颜色，再清理小区域 |
| 保持现在 | 保留当前像素图，等待最终确认 |
| 还是上一版 | 恢复真实父版本 |
| 生成PNG | 已展示当前像素版时确认并渲染同一网格 |
| 导出PDF / 给我PDF | 当前正式PNG已展示、版本清楚且无待修改时，确认PNG并只导出PDF |
| 下载清单 | 同样确认当前PNG，只导出CSV |
| PDF字大一点 | 只重新排版大字PDF，网格不变 |

返回 file_ready 后立即交付，不继续 status、inspect、全量 validate、视觉分析或自行优化。脚本必要校验已经内嵌。错误按 `code/message` 给出一个下一步，不猜 revision、不反复 stale-base 重试。复杂复合请求无法安全路由时由宿主只处理必要意图；“眼睛改绿以后再PDF”必须先编辑，不能导出旧图。

## 去杂色与关键特征保护

算法与坐标格式见[去杂色说明](references/cleanup.md)。轻度处理1–2格且色差小、周围主色明确的区域；平衡可处理部分3–5格及低频近色；强力必须有明确较低目标色数。全部保持尺寸与空格掩码。

记录的关键颜色标签可保守保护整类匹配色；还支持用户指定色号、格子、矩形区域。默认保护黑色、高对比结构、接触透明边缘的小装饰。没有语义分割，不能声称已精确识别每个眼睛或配饰。宁可少清一点，不自动删掉关键特征。

处理后展示新预览，引用 `cleanup.before/after/changed_cells/changed_percent` 的实际数值。新版本不继承旧像素或PNG确认，旧文件完整保留。

## 正式 PNG 与独立导出

最终像素确认后 `png --current` 只渲染同一网格，完整PNG与轻量预览单独交付，再 `present --artifact pattern`，等待用户确认或导出要求。不得在用户看到正式PNG前直接交付PDF/CSV。

“导出PDF”“这版就行，给我PDF”“下载清单”在上下文明确时直接确认并执行，不要求固定口令。“PDF是什么？”只解释。否定、未来计划、待修改均不导出；多版本不清楚最多问一个版本问题。

CSV/PDF独立按需生成、独立缓存、独立失败；请求一种不生成另一种。PDF沿用可读性优先的A4矢量总览、真实分页指南、施工页；提供标准清晰/大字版。78×78标准版优先2×2施工共6页，但长色号必须以可读性决定，不能为页数缩字。见[PDF说明](references/pdf-layout.md)。

## 失败、恢复、计时

取消由已有 operation/epoch 机制拒绝迟到导入，保留旧结果；不宣称可中断未知宿主图片工具。正常回合不自动返工。仅技术失败允许一次小图备用，仍失败就说明交付未完成。

本地脚本默认共享60秒预算，超时终止自有进程组。所有阶段目标均不是已验证承诺：像素/去杂色5–15秒，PNG3–10秒，CSV1–5秒，PDF1–10秒。低于目标区间下限不算失败。

`events.jsonl` 自动记录本地步骤；宿主真实时间用 `record-stage` 分别记录思考、图片工具、预览交付、脚本后等待，用 `record-delivery` 记录端到端证据。未测字段为未知，不用本地耗时填豆包耗时。记录>60秒阶段与原因，不降低尺寸、删要求或跳过校验达标。

按需读取：[快速命令](references/fast-path.md)、[迁移](references/migration.md)、[交互详情](references/interaction.md)、[下一轮豆包实测](references/doubao-tests.md)、[来源许可](references/sources.md)、[本轮验收](reports/v3-acceptance.md)。
