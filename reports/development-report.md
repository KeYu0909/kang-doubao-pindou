# 0.2.0 开发与验收报告

## 本轮实际完成

在原工程原地更名为kang-doubao-pindou，中文显示名“永康玩AI · 拼豆助手”；没有建立另一套业务项目。增加上下文交互和真实展示回执、自然确认、绑定版本的短菜单、同轮按需导出、分段计时；重新设计PDF为整体预览＋分页指南＋施工页。PNG/PDF用户指引、CSV表头与交互说明已中文化。原生豆包集成仍未实测。

原交互多问一轮的原因：SKILL把“明确确认PNG”单列成需要用户再表态的一步，底层export只认已写入的PNG确认，缺少会话展示回执与导出意图路由；不是确认函数在代码里硬编码了唯一中文口令。新版route在当前会话已实际展示有效PNG、像素已确认、版本明确、无冲突修改时，将“导出pdf”同时视为这版PNG的确认及该格式的导出。确认绑定project/revision/grid/PNG哈希、来源、时间与消息引用；不存整段私人聊天，不另调大模型分类。

否定、未来计划、疑问、只分析均不生成；改眼睛后再导出先修改；未确认像素不跳步，已确认像素但未有PNG先生成并展示PNG；未展示PNG或目标不明确只处理必要一步/一个问题。数字和推荐选项限当前菜单；“可以”在PNG阶段不猜格式；“上一版”使用真实历史文件。

## 修改前真实基线

找到了用户反馈的原10页PDF，保存在本机.baseline/v0.1/user-feedback-original.pdf，连同旧代码、SKILL、27项通过的旧测试及旧报告保留，不进发布ZIP。

该文件为78×78，MARD18色，1765颗，版本 `r1f4cb8244467`，SHA256 `f7da9c99410fb47662b389aa90c27c0df954c23d867a2414b41e81c9756fcf14`。网格哈希 `2d63950fa4bb68976ef62b2336008bc34bd3ed1d053d46a3a0f31b80455a2495`。

旧生成器固定18pt格子：A4纵向每页29列×38行，所以78行最后剩2行独占最后一排3张施工页；9张施工页后又加1张完整用量图例。旧色号字号约6.667pt。原PDF75,422字节，没有位图；不能把页多归因于重复嵌入大照片。实际查看原10页确认了末三张薄条及额外图例页。

## 同一作品最终中文PDF

| 版本 | 页数 | 纸面尺寸 | 色号字号 | 格子边长 | 文件字节数 | 本地导出含检查 |
|---|---:|---|---:|---:|---:|---:|
| 原版 | 10 | A4纵向，210×297mm | 6.667pt | 18pt | 75,422 | 本轮未重跑原文件生成，原文件时间未知 |
| 标准清晰版 | 6 | A4纵向，210×297mm | 7pt | 13.366pt | 118,062 | 1.351秒 |
| 大字施工版 | 11 | A4纵向，210×297mm | 9pt | 20.049pt | 128,673 | 1.443秒 |

标准版为1张整体、1张指南、4张39×39施工页。大字版为前2页＋9张26×26施工页。整体、指南及每页定位图共享一个矢量Form；指南表与施工共用同一分页计划，坐标从左上起、全局1基、页码从第3页开始。空格与白豆不同；覆盖每格恰好一次，不默认重叠、不改网格、不追加采购页或CSV。

同时计算A4横/纵向、边距28pt、标题/坐标/图例空间、实际最长色号字宽后选最少可读施工页。标准字号7pt，明确水平窄排90%；大字9pt不窄排。不是任意作品保证6页；五字符“WWWWW”比普通3字符宽很多，91×47压力案例标准18页、大字38页，因实际字宽需要30.736pt/44.48pt格子，不能为减页缩字或截色号。

中文正式版比原75,422字节更大：新增总览/指南/定位图/导航及嵌入中文字体。已使用矢量复用、流压缩、字体子集；页数降低不等于文件体积降低。全包中文字体子集98,236字节，来源及OFL许可见references/font-source.json与licenses/noto-cjk-OFL.txt。色号仍保持标准字体和原标识。字体构建工具只用于发布，运行不新增下载。

## 自动与视觉测试

最终本地48项测试全通过，0失败/错误/跳过，57.936秒（整套测试时间，不是单阶段）。覆盖自然意图、否定/疑问/未来/修改优先、未展示/多会话/多版本、PNG刷新保留历史并失效旧文件确认、推荐及过期菜单、版本回退、旧状态兼容、独立导出失败、模板缓存、取消与并发、真实进程超时。

PDF测试包括78×78、91×47/47×91、五字符长色号、深浅底色、透明格、四角标记与跨分区线；检查逐格有序色号、逐页全局范围、末行末列、每格恰好覆盖一次、指南链接页码、文字实际宽度及PNG同网格。每个新PDF所有页以144dpi渲染，整体页和全部施工页逐格抽取填色与源网格比对。

实际视觉查看了用户中文版标准6页和大字11页全部页面，以及合成标准/大字版全部页面接触表；放大检查施工页和长色号页。中文标题、指南、分区、范围、页脚、图例无缺字或重叠；网格完整，空白区域仍标全局坐标，没有意外空白页。合成91×47的旧版9页仅作基线，旧版未测实际长文字宽，不能把其少页当作可读性合格结论。

本轮修复过：路由timings被底层结果覆盖；修改与字号请求的优先级；疑问误触风险；五字符字宽超出原26pt格子上限；历史回退解除待修改阻塞；中文化文件变更不能沿用不同哈希的PNG确认。测试前的失败记录留在本机控制台历史，不宣称一次全过。

## 实测时间范围

1440×1920程序几何图，78×78最多18色后改12色，共3轮CLI；PNG展示/确认标记为模拟。以下包含进程启动、渲染、校验、JSON返回；不包含豆包排队/生成/上传下载/聊天交付。每项本地成功步骤小于60秒不代表豆包端到端达标。

| 本地阶段 | 最小秒 | 中位秒 | 最大秒 |
|---|---:|---:|---:|
| export-csv | 0.208 | 0.210 | 0.211 |
| export-pdf | 1.383 | 1.428 | 1.658 |
| export-pdf-cached | 0.205 | 0.206 | 0.208 |
| revision-12colors | 0.345 | 0.348 | 0.363 |
| stage1-ordinary-edit | 1.580 | 1.584 | 1.608 |
| stage1-original-candidate | 0.288 | 0.289 | 0.416 |
| stage2-78x78-18colors | 0.345 | 0.346 | 0.360 |
| stage3-png | 1.449 | 1.459 | 1.465 |

原作品标准版分段：状态检查0.0031秒，分页0.0045秒，绘制0.1774秒，逐页检查1.1636秒，文件登记0.0019秒。路由意图/确认与缓存复用细分见benchmark.json。截图中的36秒缺少当时分段证据，不能归因于某一个环节。

## 更名、兼容与打包

项目路径：`/Users/longyongkang/Documents/ChatGPT/摆花/kang-doubao-pindou`。ZIP路径：`/Users/longyongkang/Documents/ChatGPT/摆花/kang-doubao-pindou/dist/kang-doubao-pindou.zip`，顶层仅kang-doubao-pindou。用户项目仍在原工程随目录更名保留，原16份输入/网格/PNG/PDF文件在第一轮升级中校验不变；中文刷新前18份历史文件也逐一核对未改。新中文PNG另存，原确认进入历史，网格不变。

代码继续读取schema1；新增会话展示/菜单和偏好为可选字段，旧项目skill_version保持真实创建值；新PDF缓存含overview-guide-build-v3-zh，旧v1/v2PDF可校验。旧Skill安装不会自动删除；新版明确调用kang-doubao-pindou，同作品不同时混用两个版本。完整替换步骤见references/migration.md。

安装：Python3.10+建.venv → `python -m pip install --require-hashes -r requirements.lock` → doctor。测试：`python scripts/run_tests.py`；PDF对照：`python scripts/pdf_benchmark.py`；打包：`python scripts/package_skill.py`；独立验收：`python scripts/verify_package.py --zip dist/kang-doubao-pindou.zip`。命令均在技能根目录用.venv/bin/python执行。

包按白名单收录代码/文档/测试/色卡/合法开源字体与许可，排除用户图、基线、runs、密钥、私有字体、.research、虚拟环境和缓存。MANIFEST逐文件SHA256。最终ZIP独立解压/新环境安装/全测试/完整模拟CLI/重打包结果记录在包外reports/clean-room.json，绑定最终ZIP哈希，避免把包本身哈希递归写回ZIP。

## 已知边界与豆包下一步

没有访问豆包客户端，不声称原生导入、脚本执行、实际按钮、图片工具、中断或端到端60秒已验证。本轮无收费生图、私人图上传、网站部署或线上业务修改。规则路由是保守短语识别，复杂表达仍由当前宿主理解，不能靠额外模型分类或绕过状态保护。

豆包实测先核实真实导入和执行能力；实际展示PNG后仅说“导出pdf”，应同轮完成；另测“不要导出”、修改后再请求、标准转大字及所有页面对应关系。没有执行能力时本机脚本＋手动附件回传，记录manual-fallback。完整对话和记录模板见references/doubao-tests.md。
